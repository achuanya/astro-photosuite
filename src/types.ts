export interface PhotosuiteOptions {
  selector?: string;
  gallery?: string;
  glightbox?: boolean;
  imageAlts?: boolean;
  exif?: boolean;
  imageBase?: string;
  imageDir?: string;
  fileDir?: boolean;
}
